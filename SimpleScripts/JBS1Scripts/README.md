# JBS-1 Script Collection

Plain ECMAScript scripts for the first post-JBS-0 layer. These scripts exercise
protocol-view behavior and Object prototype methods while staying inside the
current parser subset.

Run:

```sh
cargo test --test jbs1_script_collection
```
