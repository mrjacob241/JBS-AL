# JBS-7 SimpleScripts

Fifty executable scripts for the JBS-7 stage: property deletion, `in`,
`instanceof`, script constructors, `for...in`, Array iteration methods, native
error constructors, `Test262Error`, `AggregateError`, modulo, update operators,
conditionals, comma expressions, and declaration forms.
